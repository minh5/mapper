from django import forms

# from .models import DataFile

# class DataForm(forms.ModelForm):
#     class Meta:
#         model = DataFile
#         fields = ('name', 'uploaded_file')

    # match_key = forms.CharField(max_length=50)
    # upload_file = forms.FileField(label='Upload CSV File')
    # min_color = forms.CharField(max_length=50)
    # max_color = forms.CharField(max_length=50)
